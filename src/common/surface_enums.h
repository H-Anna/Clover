#ifndef SURFACE_ENUMS_H
#define SURFACE_ENUMS_H

enum DrawMethod
{
    Base,
    Overlay,
    Clip,
    Replace,
    Insert
};

enum Frequency
{
    Never,
    Always,
    Loop
};

#endif // SURFACE_ENUMS_H
